export enum Prefix {
  API = 'api',
  Auth = 'auth',
  User = 'user'
}

export enum Path {
  Regster = 'register',
  Spec = 'spec',
  Login = 'login',
  ID = ':id',
}

export enum Field {
  ObjectID = '_id',
  ID = 'id'
}

export enum Title {
  Users = 'Users service'
}

export enum Desc {
  Users = 'Users service API'
}

export enum Version {
  Users = '1.0'
}

export enum Info {
  AppRun = '🚀 Application is running on:  http://localhost:'
}
